<p style="color: green"><%= notice %></p>

<%= render @operation %>

<div>
  <%= link_to "Редагувати", edit_operation_path( @operation), class: "btn btn-success btn-sm" %> 
  
  <%= button_to "Видалити", [@category, @operation], method: :delete, class: 'btn btn-danger btn-sm'%>
  <%= link_to "Back to operations", operations_path, class: "btn btn-secondary" %>
</div>
